# Python dependencies for the analysis in this repository.
# Versions are pinned to those used to produce the reported results
# (Python 3.14.2, macOS). Generated from the imports actually present
# in the scripts, not from a general-purpose environment dump.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- core numerics / data handling -------------------------------------
numpy==2.4.1
scipy==1.17.0
pandas==2.3.3
openpyxl==3.1.5          # reading/writing .xlsx

# --- statistics ---------------------------------------------------------
statsmodels==0.14.6      # regression, ANOVA
pingouin==0.5.5          # repeated-measures ANOVA, pairwise tests
scikit-posthocs==0.11.4  # Dunn's post-hoc
scikit-learn==1.8.0      # k-means, PCA, cluster validity indices

# --- figures ------------------------------------------------------------
matplotlib==3.10.8
seaborn==0.13.2

# Note: `tkinter` is imported by two scripts for interactive file selection.
# It ships with CPython on macOS/Windows; on Debian/Ubuntu install the
# system package `python3-tk`. It is not installable via pip.
